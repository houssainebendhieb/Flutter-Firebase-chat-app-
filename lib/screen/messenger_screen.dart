import 'package:flutter/material.dart';

class MessengerScreen extends StatelessWidget {
  MessengerScreen({super.key});

  Widget build(context) {
    return Text('welcome your connected');
  }
}
